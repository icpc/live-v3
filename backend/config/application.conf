ktor {
    deployment {
        port = 8080
        port = ${?PORT}
    }
    application {
        modules = [ org.icpclive.ApplicationKt.module ]
    }
}
auth {
    disabled = true
}
credentials {
    include "creds.conf"
}
live {
# relative from backend directory
    configDirectory = "config/archive/rmc21"
# relative from config directory
    presetsDirectory = presets
    mediaDirectory = media
}
