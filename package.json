{
  "name": "icpc-live-v3",
  "version": "3.1.0",
  "private": true,
  "scripts": {
    "buildOverlay": "npm run build -w overlay",
    "buildAdmin": "npm run build -w admin"
  },
  "workspaces": [
    "overlay",
    "admin"
  ]
}
